/* eslint-disable */

// ESLint is configured for NodeJS and not for javascript
console.log('hello from the client side :D');
const locations = JSON.parse(document.getElementById('map').dataset.locations);